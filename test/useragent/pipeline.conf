filter {
    useragent {
      source => "[user_agent][original]"
      ecs_compatibility => "disabled"
    }

    useragent {
      source => "[user_agent][original]"
      target => "pizza"
      ecs_compatibility => "disabled"
    }

    useragent {
      source => "[user_agent][original]"
      ecs_compatibility => "v8"
    }
}